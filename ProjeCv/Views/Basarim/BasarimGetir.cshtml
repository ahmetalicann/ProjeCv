@model ProjeCv.Models.Entity.TBLAWARDS

@{
    ViewBag.Title = "BasarimGetir";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

@using (Html.BeginForm("Guncelle", "Basarim", FormMethod.Post))
{
    <div class="form-group">
        @Html.LabelFor(m => m.ID)
        @Html.TextBoxFor(m => m.ID, new { @class = "form-control" })
        <br />
        @Html.Label("BAŞARIM")
        @Html.TextBoxFor(m => m.AWARD, new { @class = "form-control" })
        <br />
        <button class="btn btn-primary">Başarım Güncelle</button>
    </div>
}

